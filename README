qtlogwatcher readme

Overview:

QtLogwatcher is a graphical Logwatcher client.
It's built with the Qt Window Framework. 


Author:

Sven Sperner		<cethss@gmail.com>


Checkout:

git clone https://github.com/sperner/QtLogwatcher


Compile (standard way):

qmake
make
make install


Compile (arch build system):

cp PKGBUILD $anywhere
cd $anywhere
makepkg


Qt-Creator:

This project was developed using the Qt-Creator.
The project file is included in the package.


Configurations:

Configuration file:	qtlogwatcher.conf
Configurable through the clients settings menu


Usage:

qtlogwatcher
